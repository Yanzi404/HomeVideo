# PRD：HomeVideo — 家庭内网视频播放系统

**版本**：v2.0
**作者**：[待填写]
**日期**：2026-09-10
**状态**：草稿

---

## 1. 背景与目标

### 1.1 背景

家庭视频内容（电影、剧集、家庭录像等）集中存储在家里一台 Linux 服务器（s2）的硬盘上。现有通用播放器（如 Kodi、VLC）功能臃肿、配置复杂，对非技术家庭成员不友好。

需要一个**开箱即用、操作简单**的家庭视频播放系统，采用前后端分离架构：服务端部署在 Linux 服务器上管理视频资源，客户端运行在索尼电视上提供浏览和播放体验。

### 1.2 目标用户

| 用户角色 | 特征描述 | 核心诉求 | 使用场景 |
|---------|---------|---------|---------|
| 家庭管理者（配置者） | 有一定技术能力，负责维护家庭服务器 | 一次部署好服务端，客户端配置简单 | 首次安装时部署服务端、在电视上配置服务器地址 |
| 家庭观看者 | 老人、小孩、其他家庭成员，技术水平不一 | 打开就能看，操作直观，不需要学习 | 晚饭后在电视上选一部电影、翻看家庭录像 |

### 1.3 业务目标与成功指标

| 目标 | 衡量指标 | 目标值 | 监测方式 |
|------|---------|--------|---------|
| 易用性 | 从安装到首次播放视频的耗时 | < 3 分钟 | 自测 + 家庭成员体验反馈 |
| 稳定性 | 视频播放无崩溃率 | > 95% | 本地日志统计 |
| 家庭满意度 | 非技术家庭成员能否独立使用 | 所有家庭成员可独立操作 | 实际使用观察 |

---

## 2. 需求概述

HomeVideo 采用**前后端分离**架构：

- **服务端**：Java（Spring Boot）应用，部署在家庭 Linux 服务器（s2, 192.168.31.73）上，直接读取本地硬盘视频文件，提供 REST API 和 HTTP 视频流传输
- **客户端**：Android TV 应用，运行在索尼 BRAVIA 4K VH22（Android TV 10）上，通过 HTTP 与服务端通信，浏览视频并播放

仅在内网通信，无需外网、无需注册账号。

---

## 3. 架构设计

### 3.1 系统架构

```
┌─────────────────────────────────────────────────────┐
│  s2 · Linux 服务器（192.168.31.73）                   │
│                                                     │
│  ┌──────────────────────┐    ┌──────────────────┐   │
│  │  Java 服务端          │    │  本地硬盘          │   │
│  │  Spring Boot (:8080) │───▶│  /srv/hometok-   │   │
│  │                      │    │  media/           │   │
│  │  · 文件浏览 API       │    │  （视频文件目录）    │   │
│  │  · 视频流传输         │    └──────────────────┘   │
│  │  · 字幕匹配与提供     │                           │
│  │  · 缩略图生成         │                           │
│  │  · 播放历史存储       │                           │
│  └──────────┬───────────┘                           │
│             │ HTTP (LAN)                             │
└─────────────┼───────────────────────────────────────┘
              │
              ▼
┌─────────────────────────────────────────────────────┐
│  索尼 BRAVIA 4K VH22（Android TV 10）               │
│                                                     │
│  ┌──────────────────────┐                           │
│  │  Android TV 客户端    │                           │
│  │                      │                           │
│  │  · 文件浏览界面       │                           │
│  │  · ExoPlayer 播放    │                           │
│  │  · 遥控器操控         │                           │
│  │  · 播放历史（本地缓存）│                           │
│  └──────────────────────┘                           │
└─────────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 组件 | 技术 | 说明 |
|------|------|------|
| 服务端框架 | Java 17 + Spring Boot 3.x | 成熟稳定，生态丰富 |
| 服务端数据库 | H2（文件模式） | 轻量嵌入式，存储播放历史和配置 |
| 视频元数据 | ffmpeg（进程调用） | 提取分辨率、时长、音轨信息 |
| 缩略图生成 | ffmpeg（帧截取） | 从视频中截取关键帧 |
| 客户端语言 | Kotlin | Android 官方推荐 |
| 客户端 UI | Jetpack Compose for TV | 现代声明式 UI，TV 原生支持 |
| 播放内核 | Media3 ExoPlayer | 官方推荐，格式支持好 |
| 网络请求 | Retrofit + OkHttp | REST API 调用 |
| 最低 API Level | 29（Android TV 10） | 匹配目标设备 |

### 3.3 通信协议

客户端与服务端通过 HTTP 通信，所有接口返回 JSON，视频流支持 HTTP Range 请求。

**核心 API 概览**：

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/files` | GET | 浏览目录，返回文件和文件夹列表 |
| `/api/files/search` | GET | 按文件名搜索 |
| `/api/videos/stream/{id}` | GET | 视频流传输（支持 Range） |
| `/api/videos/info/{id}` | GET | 视频详情（分辨率、时长、音轨、字幕列表） |
| `/api/videos/subtitles/{id}` | GET | 获取指定字幕文件内容 |
| `/api/videos/thumbnail/{id}` | GET | 获取视频缩略图 |
| `/api/history` | GET/POST/DELETE | 播放历史增删查 |

---

## 4. 功能详细设计

### 4.1 服务端：文件浏览与搜索（P0）

**功能描述**：服务端读取本地视频目录，提供文件浏览和搜索 API。

**业务规则**：
1. 视频根目录可配置（默认 `/srv/hometok-media`），支持多个子目录
2. 按文件夹层级展示，与服务器目录结构一致
3. 自动过滤非视频文件（仅返回 .mp4, .mkv, .avi, .mov, .ts, .flv, .wmv, .rmvb 等常见视频格式）
4. 视频文件返回：文件名、文件大小、分辨率（通过 ffmpeg 提取）、修改时间
5. 支持按文件名关键字搜索（模糊匹配）
6. 列表排序：按文件名（默认）、按修改时间（最新优先）
7. 支持分页（默认每页 100 条），大目录不卡顿

**API 设计**：

```
GET /api/files?path=/movies&sort=name&order=asc&page=0&size=100
→ { "path": "/movies", "folders": [...], "files": [...] }

GET /api/files/search?keyword=家庭
→ { "results": [{ "name": "家庭聚会.mp4", "path": "/home/家庭聚会.mp4", ... }] }
```

---

### 4.2 服务端：视频流传输与信息（P0）

**功能描述**：服务端提供视频文件的 HTTP 流式传输，支持断点续传和随机访问；同时提供视频元数据和缩略图。

**业务规则**：
1. 视频流支持 HTTP Range 请求，客户端可任意 seek 无需重新下载
2. 视频信息通过 ffmpeg 提取：时长、分辨率、编码格式、可用音轨列表、可用字幕轨道列表
3. 缩略图自动截取视频第 10% 处的帧，生成 JPEG 缓存到本地
4. 字幕自动匹配：扫描视频同目录下同名 .srt、.ass 文件，返回可用字幕列表
5. 字幕支持 UTF-8 和 GBK 编码，服务端统一转为 UTF-8 返回
6. 视频流传输使用 `application/octet-stream`，支持大文件（>10GB）

**API 设计**：

```
GET /api/videos/stream/{id}
  Range: bytes=0-1048575
→ 206 Partial Content (视频二进制流)

GET /api/videos/info/{id}
→ { "name": "movie.mkv", "duration": 7200, "resolution": "1920x1080",
    "audioTracks": [...], "subtitles": [...] }

GET /api/videos/thumbnail/{id}
→ image/jpeg

GET /api/videos/subtitles/{id}?index=0
→ text/plain (UTF-8 字幕内容)
```

---

### 4.3 服务端：播放历史（P1）

**功能描述**：服务端存储播放历史，支持多设备共享和断点续播。

**业务规则**：
1. 记录每个视频的播放位置、最后播放时间、播放时长
2. 播放不足 30 秒的视频不记录
3. 最多保留最近 50 条记录
4. 支持清除单条记录和全部历史
5. 播放历史存储在服务端 H2 数据库中，数据持久化

**API 设计**：

```
POST /api/history
→ { "videoId": "...", "position": 1800, "duration": 7200 }

GET /api/history
→ [{ "videoId": "...", "videoName": "movie.mkv", "position": 1800,
     "duration": 7200, "lastPlayed": "2026-09-10T20:30:00",
     "thumbnailUrl": "/api/videos/thumbnail/..." }]

DELETE /api/history/{videoId}
DELETE /api/history
```

---

### 4.4 客户端：服务器连接与配置（P0）

**功能描述**：客户端连接服务端，配置一次后自动重连。

**业务规则**：
1. 自动扫描局域网内在 8080 端口运行的服务端（子网广播或 mDNS）
2. 支持手动输入服务器 IP 地址连接（当自动发现失败时）
3. 配置完成后，后续打开 App 自动连接，无需重复配置
4. 连接信息本地存储（DataStore），无需认证（内网信任）

**交互流程**：
1. 首次启动进入「连接服务器」引导页
2. 点击「自动扫描」，展示发现的服务端（含 IP）
3. 选择或手动输入 IP → 连接成功 → 直接进入主界面
4. 后续可在「设置」中修改服务器地址

**异常处理**：

| 异常场景 | 处理方式 | 用户提示 |
|---------|---------|---------|
| 扫描超时（10秒无响应） | 停止扫描，引导手动输入 | "未发现服务端，可手动输入服务器地址" |
| 手动输入 IP 无法连接 | 提示检查网络和服务端 | "无法连接到服务端，请确认服务端已启动" |
| 服务端离线 | 主界面顶部显示连接状态 | "服务端未连接"，提供重连按钮 |

---

### 4.5 客户端：视频浏览与搜索（P0）

**功能描述**：以文件管理器的方式展示服务端上的视频文件，支持文件夹层级浏览和按文件名搜索。

**业务规则**：
1. 主界面直接展示服务端视频根目录的文件列表
2. 按文件夹层级展示，与服务端目录结构一致
3. 视频文件显示：文件名、文件大小、分辨率
4. 支持按文件名关键字搜索（遥控器输入）
5. 列表排序方式：按文件名（默认）、按修改时间（最新优先）
6. 记住每个文件夹的上次浏览位置

**交互流程**：
1. 打开 App → 主界面直接展示服务端视频根目录
2. 点击文件夹 → 进入下一级
3. 点击视频文件 → 进入播放
4. 按遥控器「搜索」键 → 弹出搜索框 → 输入关键字 → 实时过滤结果
5. 按遥控器「返回」键 → 逐级返回上一级

**异常处理**：

| 异常场景 | 处理方式 | 用户提示 |
|---------|---------|---------|
| 服务端连接中断 | 自动重试一次，失败后提示 | "与服务端连接断开，请检查网络" |
| 文件夹内容为空 | 显示空状态 | "该文件夹中没有视频文件" |
| 搜索无结果 | 显示空状态 | "没有找到匹配的视频" |

---

### 4.6 客户端：视频播放（P0）

**功能描述**：播放选中的视频文件，提供完整的播放控制能力。

**业务规则**：
1. 支持的视频格式：H.264/H.265 编码的 MP4/MKV 为主，兼容 AVI、MOV、TS 等
2. 支持的音频格式：AAC、AC3、DTS（如电视硬件支持）
3. 播放控制：播放/暂停、快进/快退（10秒步进）、跳转到指定时间
4. 长按快进/快退键：3倍速快进/快退
5. 支持切换音轨（多音轨文件）
6. 支持切换字幕轨道、调整字幕大小、关闭字幕
7. 支持调节播放速度：0.5x / 1.0x / 1.25x / 1.5x / 2.0x
8. 自动记住每个视频的播放位置，下次打开从断点续播
9. 播放结束前最后 30 秒，显示「重新播放」和「返回」快捷按钮
10. 播放界面默认 5 秒后自动隐藏控制栏，按遥控器任意键重新显示

**交互流程**：
1. 在文件列表中选中视频 → 点击播放 → 全屏播放
2. 播放中按「确认」键 → 显示底部控制栏（进度条、当前时间/总时长、播放速度、音轨、字幕）
3. 方向键左/右 → 快退/快进 10 秒
4. 长按左/右 → 3 倍速快退/快进
5. 按「菜单」键 → 弹出设置面板（音轨选择、字幕选择、播放速度）
6. 按「返回」键 → 隐藏控制栏 / 第二次按退出播放
7. 播放结束 → 显示「重新播放」「返回列表」选项

**异常处理**：

| 异常场景 | 处理方式 | 用户提示 |
|---------|---------|---------|
| 视频格式不支持 | 停止播放，返回文件列表 | "该视频格式暂不支持播放" |
| 播放中网络中断 | 暂停播放，尝试重连 5 秒 | "网络连接中断，正在重试..."；失败后提示"播放已暂停，请检查网络" |
| 字幕文件编码异常 | 尝试 UTF-8 / GBK 自动检测 | 无法识别时提示"字幕文件无法正确显示" |
| 播放卡顿/缓冲 | 显示缓冲提示 | "正在缓冲..." |

---

### 4.7 客户端：播放历史（P1）

**功能描述**：在主界面展示最近播放过的视频，方便快速继续观看。

**业务规则**：
1. 在主界面顶部展示「最近观看」区域，最多显示 10 条
2. 每条记录显示：视频缩略图（从服务端获取）、文件名、上次播放到的位置、播放时间（如"昨天"、"3天前"）
3. 点击记录直接从断点续播
4. 支持清除单条记录（长按弹出菜单）
5. 支持清除全部历史记录（在设置中）
6. 播放不足 30 秒的视频不记录

**交互流程**：
1. 打开 App → 主界面顶部「最近观看」横向滚动列表
2. 选中某个视频 → 点击 → 从上次位置继续播放
3. 长按某条记录 → 弹出「从列表中移除」选项

**异常处理**：

| 异常场景 | 处理方式 | 用户提示 |
|---------|---------|---------|
| 历史记录对应的视频文件已被删除或移动 | 点击时提示 | "该视频文件已不存在" |
| 服务端离线导致历史记录不可达 | 记录置灰 | "服务端未连接，请先连接" |

---

### 4.8 客户端：设置（P1）

**功能描述**：提供必要的应用配置项。

**业务规则**：
1. 服务器连接：修改服务器 IP，测试连接
2. 播放设置：默认播放速度、字幕大小（小/中/大）
3. 关于：App 版本号
4. 清除播放历史

**交互流程**：
1. 主界面导航到「设置」入口
2. 设置项以列表形式展示，方向键选择进入
3. 修改后即时生效，按返回键保存并退出

---

## 5. 非功能需求

| 类别 | 要求 | 验收标准 |
|------|------|---------|
| 性能 | 文件列表加载时间 | < 3 秒（1000 个文件以内） |
| 性能 | 视频起播时间（从点击到画面出现） | < 2 秒（内网环境） |
| 性能 | App 启动到主界面可用 | < 2 秒 |
| 兼容性 | 索尼 BRAVIA 4K VH22（Android TV 10, API 29） | 所有功能正常运行 |
| 兼容性 | 遥控器操作 | 所有交互均可仅通过遥控器方向键+确认键完成 |
| 兼容性 | 屏幕适配 | 适配 1080p 和 4K 电视屏幕 |
| 安全 | 网络通信 | 仅在家庭内网通信，不请求外网权限 |
| 稳定性 | 长时间播放 | 连续播放 4 小时无内存泄漏或崩溃 |
| 部署 | 服务端部署 | 支持 systemd 管理，开机自启动 |

---

## 6. 数据需求

所有数据仅存储在本地服务端，不上传任何信息。

| 事件名 | 触发条件 | 关键属性 | 用途 |
|--------|---------|---------|------|
| server_connected | 客户端成功连接服务端 | 服务器 IP | 了解连接状态 |
| video_browse | 进入某个文件夹 | 文件夹路径、文件数量 | 了解浏览行为 |
| video_play_start | 开始播放一个视频 | 文件名、分辨率 | 统计播放行为 |
| video_play_end | 结束播放（含正常结束和退出） | 文件名、播放时长、播放进度百分比 | 分析观看完成率 |
| video_play_error | 播放出错 | 错误类型、文件名 | 定位兼容性问题 |
| search_performed | 执行一次搜索 | 搜索关键字、结果数量 | 了解搜索使用频率 |

---

## 7. 验收标准

| 编号 | 场景 | Given（前提） | When（操作） | Then（预期结果） |
|------|------|-------------|-------------|-----------------|
| AC-01 | 首次配置 | App 刚安装，首次启动 | 按引导连接服务端 | 3 分钟内完成配置，进入主界面看到文件列表 |
| AC-02 | 浏览视频 | 已连接服务端 | 在主界面逐级进入文件夹 | 正确展示文件夹结构和视频文件列表 |
| AC-03 | 播放视频 | 文件列表中有视频文件 | 选中视频并点击播放 | 2 秒内开始播放，画面和声音正常 |
| AC-04 | 播放控制 | 视频正在播放 | 按遥控器左右键 | 快进/快退 10 秒，进度条更新 |
| AC-05 | 断点续播 | 某视频上次播放到 30:00 位置 | 再次打开该视频 | 自动从 30:00 位置开始播放 |
| AC-06 | 字幕加载 | 视频同目录下有同名 .srt 字幕 | 播放该视频 | 自动加载并显示字幕 |
| AC-07 | 网络断开 | 视频正在播放 | 服务端突然离线 | 暂停播放并提示网络中断 |
| AC-08 | 服务端离线 | 服务端已停止 | 打开 App | 显示"服务端未连接"，提供重连入口 |
| AC-09 | 搜索视频 | 服务端上有视频文件 | 输入关键字搜索 | 实时过滤显示匹配结果 |
| AC-10 | 最近观看 | 已播放过几个视频 | 查看主界面「最近观看」 | 正确展示最近 10 个视频，点击可续播 |
| AC-11 | 遥控器操作 | App 任意界面 | 仅使用方向键+确认键+返回键 | 可完成所有操作，焦点移动合理 |

---

## 8. 排期建议

| 阶段 | 预估工时 | 依赖 | 风险点 |
|------|---------|------|--------|
| 服务端：Spring Boot 骨架 + 文件浏览 API | 3 天 | 无 | 无 |
| 服务端：视频流传输 + 字幕 + 缩略图 | 4 天 | 文件浏览 | ffmpeg 依赖安装，大文件 seek 性能 |
| 服务端：播放历史 API | 2 天 | 骨架 | 无 |
| 客户端：项目骨架 + 服务器连接 + 引导 | 4 天 | 服务端 API 定义 | 服务端发现机制 |
| 客户端：文件浏览界面 | 2 天 | 服务器连接 | 大列表性能 |
| 客户端：视频播放器 + 控制 | 4 天 | 文件浏览 | ExoPlayer HTTP 流适配 |
| 客户端：播放设置 + 字幕 | 2 天 | 播放器 | 遥控器按键适配 |
| 客户端：播放历史 + 设置 | 2 天 | 播放器 | 无 |
| 联调 + 兼容性测试 | 3 天 | 全部功能 | 索尼电视真机适配 |
| 家庭成员体验测试 + 修复 | 2 天 | 联调完成 | 非技术用户操作习惯差异 |
| **合计** | **约 25 天（1 人）** | | |

**注**：服务端和客户端可在 API 契约确定后并行开发，关键路径为客户端。

---

## 9. 风险与依赖

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|---------|
| 索尼电视硬件解码限制（部分编码不支持硬解） | 中 | 中 | 使用 ExoPlayer 并优先硬解，不支持时回退软解 |
| 大文件（>10GB）播放 seek 卡顿 | 低 | 中 | 服务端使用 HTTP Range + 流式传输，避免全文件加载 |
| ffmpeg 在服务端性能不足（缩略图生成慢） | 低 | 低 | 缩略图缓存到本地，仅首次生成时耗时 |
| 遥控器型号差异导致按键映射不一致 | 低 | 低 | 使用 Android TV 标准 KeyEvent，覆盖索尼遥控器测试 |
| 服务端 Java 版本兼容性 | 低 | 中 | 使用 Java 17 LTS，Spring Boot 3.x 官方支持 |

---

## 10. 部署方案

### 10.1 服务端部署

```bash
# 服务端以 jar 包形式运行，通过 systemd 管理
# 1. 安装 Java 17 + ffmpeg
sudo apt install openjdk-17-jre-headless ffmpeg

# 2. 部署 jar
cp homevideo-server.jar /opt/homevideo-server/

# 3. 配置 systemd 服务
# /etc/systemd/system/homevideo-server.service
[Service]
ExecStart=/usr/bin/java -jar /opt/homevideo-server/homevideo-server.jar \
  --homevideo.media-dir=/srv/hometok-media \
  --server.port=8080

# 4. 启动并设置开机自启
sudo systemctl enable --now homevideo-server
```

### 10.2 客户端部署

```bash
# 生成 APK 后安装到电视
adb install homevideo-client.apk
```

---

## 附录

### 竞品参考

| 产品 | 优势 | 不足（本产品改进方向） |
|------|------|---------------------|
| Kodi | 功能全面，格式支持广 | 配置复杂，界面信息密度高，非技术用户难以上手 |
| VLC for Android TV | 格式兼容性极好 | 浏览体验一般，无视频源管理，界面不够 TV 友好 |
| Nova Video Player | 自动刮削海报，界面美观 | 配置步骤多，对外网有依赖（刮削元数据） |
| nPlayer | 播放能力强 | 收费，界面设计偏移动端 |

### v1.1 → v2.0 变更说明

| 变更项 | v1.1（单 App 直连 SMB） | v2.0（前后端分离） | 影响 |
|--------|------------------------|-------------------|------|
| 架构 | 单 Android App，通过 smbj 直连 SMB | Java 服务端 + Android TV 客户端 | 新增服务端开发 |
| 协议 | SMB/CIFS (smbj) | HTTP REST API + 视频流 | 砍掉 smbj 依赖 |
| 文件访问 | 客户端通过 SMB 读取 | 服务端直接读本地文件系统 | 更高效，无 SMB 协议开销 |
| 播放历史 | 存储在客户端本地 | 存储在服务端（可多设备共享） | 需开发服务端持久化 |
| 部署 | 仅安装 APK | 部署服务端 jar + 安装 APK | 多一步服务端部署 |
| Story 数量 | 12 个 | 15 个 | 增加 3 个（服务端相关） |
| 总 Points | 44 | 55 | 增加 11 点 |
| 预估工期 | 20 天 | 25 天 | 增加 5 天（可并行缩短） |
